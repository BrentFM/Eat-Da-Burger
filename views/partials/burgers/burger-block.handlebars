<li>
	{{burger_name}}
	<button class="btn btn-danger delete-burger" data-id="{{this.id}}">DELETE!</button>

		<button class="btn btn-dark change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Eat me!{{else}}Regurgitate!{{/if}}
	</button>
</li>
